\begin{ericbox}{cut plan for this section: 4,740 $\to$ 1,300 words; appendices $\to$ online}
The RP version keeps a robustness section built around one summary table
--- variant, baseline, variant value, verdict --- drawn from the existing
toggled-parameter matrix, plus short paragraphs on the four checks a
referee will actually probe: the four builds (representation
$\times$ weighting), the resolution sweep with the seed-stability
resolution of it, the decayed-window attenuation (the one estimate that
moves), and the unconditional composite. Everything else --- window
width and mix, functional forms, listing-vs-reporting, the small-residual
subsection, term scoring at the firm level --- compresses to one
sentence each pointing at the online appendix, where the full text
already lives.

The paper appendices (corpus and burn-in; registration profiles; refile
figure; representation surface; the three encodings) all move to the
online appendix, except a half-page data appendix RP will want in the
manuscript: corpus, scoring parameters, and sample definitions in one
table. The burn-in figure survives inside it.
\end{ericbox}
